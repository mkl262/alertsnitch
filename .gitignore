# Binaries
# Anchored to the repo root so it ignores the built ./alertsnitch binary but NOT
# the helm/alertsnitch/ chart directory (a bare "alertsnitch" matched both).
/alertsnitch
alertsnitch-*
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool
coverage.out
coverage.html
*.coverprofile

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Temporary files
tmp*
*.tmp
*.log
*.tar

# Go workspace
go.work
go.work.sum

# Dependency directories
vendor/

# Build output
dist/
bin/

# goreleaser
dist/

# Environment files (may contain secrets)
.env
./example/.env
.env.local
.env.*.local

# Air (live reload)
tmp/

# Debug
__debug_bin*
