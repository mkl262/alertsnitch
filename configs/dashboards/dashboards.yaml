apiVersion: 1

providers:
  - name: 'Loki / Alert History'
    orgId: 1
    folder: 'Alert History'
    folderUid: ''
    type: 'file'
    disableDeletion: true
    editable: true
    updateIntervalSeconds: 10
    allowUiUpdates: false
    options:
      path: /etc/grafana/dashboards